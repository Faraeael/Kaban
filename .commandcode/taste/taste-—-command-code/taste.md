# Taste — Command Code
- Develops on Windows (paths like `C:\Project\...`, uses `dir`, `copy`, `del`, `findstr` in shell). Use Windows-compatible shell commands and path separators. Confidence: 0.9
- Uses Flutter with Riverpod for state management (`flutter_riverpod`). Confidence: 0.9
- Uses `sqflite` for local persistence. When local `Transaction` model exists, prefer `hide Transaction` on the sqflite import to avoid name collision. Confidence: 0.85
- Uses `go_router` for navigation. Confidence: 0.85
- Uses `uuid` for ID generation in domain models. Confidence: 0.8
- Organizes Flutter projects into `lib/{data,models,screens,state,services,router,utils}` folders. Confidence: 0.8
- Prefers autonomous execution: after approving a high-level plan ("Recommended" option), wants the agent to continue fixing issues end-to-end without re-asking permission for each step. Short replies like "Continue" mean "proceed with your plan"; wholesale approvals like "Aight do all of that" over a whole prioritized roadmap (including newly bundled bug reports) mean run the entire batch. Confidence: 0.9
- Wants verification after fixes: prefers running `flutter analyze` and `flutter test` to confirm the project actually compiles and passes tests, not just reducing error count. Confidence: 0.9
- For Flutter scaffold repairs: back up custom Android native files (AndroidManifest.xml, MainActivity.kt, custom services) before running `flutter create`, then restore them after. Confidence: 0.85
- Prefers fixing root causes (missing imports, type collisions, broken syntax) over silencing lints. Confidence: 0.8
- Accepts concise summaries at the end with a bullet list of what was fixed and final verification status. Confidence: 0.8
- Doesn't need lengthy explanations of well-understood Flutter/Dart concepts — gets straight to diagnosis and action. Confidence: 0.75
- Wants fully lint-clean code: "No issues found" from `flutter analyze` is the target, not just zero errors. Surface previously-existing warnings (unused fields, prefer_const, unnecessary braces, curly_braces_in_flow_control, etc.) and fix them. Confidence: 0.8
- Prefers making a state field actually used (e.g., surfacing `_error` as a dismissible UI banner) over suppressing warnings with `// ignore:` comments or leaving dead code. Confidence: 0.8
- Communicates in casual, lowercase, terse tone ("Aight now...", short imperative sentences). Match the register — no need for formal preamble. Confidence: 0.9
- Wants first-run onboarding to surface advanced setup that would otherwise be buried in Settings (e.g., adding CommandCode/API key during onboarding, not just expecting the user to find it later). Onboarding should cover: welcome, primary entity creation, and integration/API setup. Confidence: 0.8
- Treats in-app instructions / "How to use" screens as a first-class feature, not an afterthought. Adds them when shipping new app scope and expects them to be reachable from Settings (and re-runnable onboarding). Confidence: 0.8
- Uses the `impeccable` design skill at `C:/Users/Pia Romualdo/.agents/skills/impeccable/` (loaded via `node scripts/context.mjs`). Has `reference/onboard.md` and `reference/craft-floor.md` for design principles. Load and follow its guidance when redesigning UI flows. Confidence: 0.85
- After UI/design changes, runs the mechanical design detector (`node scripts/detect.mjs`) over the touched files to verify against the floor rules. Treats clean detector output as part of "done". Confidence: 0.8
- Onboarding should follow "operate mode" principles: get to value fast, prefer empty-state-as-onboarding patterns over ceremony, use progressive disclosure (e.g., only show API key field when remote provider is selected, not as a first-class obstacle). Skip button always available. Confidence: 0.85
- Avoids generic icon+heading+text feature-list card patterns in onboarding/welcome screens — reads as a category template. Replaces with live previews pulling real values from state providers (e.g., dashboard preview with actual net worth, wallet count, income/expense) so users see their future screen, not a brochure. Confidence: 0.85
- Help/instructions screens should be task-organized ("I want to add a wallet") not feature-organized ("Wallets"). Each task gets numbered steps and a deep-link button to the relevant screen. Include a search field for "What are you trying to do?" and a proper empty state when no matches. Confidence: 0.85
- Adds `HapticFeedback.selectionClick()` on page changes, tile selections, and navigation; `HapticFeedback.lightImpact()` on completion actions. Confidence: 0.7
- Values friction-reducing UX around API key / integration setup: a "Test API" button that fires a real connectivity check (e.g., minimal 4-token "Reply with OK" ping) to verify key + base URL + model end-to-end. Returns a clear status card with the actual response/error and round-trip time, not a vague "saved". Confidence: 0.8
- Auto-resets test/validation result state when underlying inputs (key, URL, model) change so the user isn't shown stale results after editing. Confidence: 0.7
- Prefers asking a single focused design question via `ask_user_question` with 2-3 concrete options (with "Recommended" badge) before building, when there's a real product tradeoff the user should decide. Confidence: 0.75
- When integrating with third-party proxy APIs (e.g., CommandCode's `/provider/v1`), never strip or normalize upstream identifiers (model IDs, route slugs, etc.) without verifying they still resolve. Proxies route by full namespaced ID — sending a stripped ID silently returns "model not supported" instead of a clear error. Always cross-check curated IDs against the authoritative `/models` or `/routes` listing before committing them. Confidence: 0.85
- When tests fail unexpectedly or behavior is unclear, debug empirically instead of guessing: write a quick throwaway repro script (e.g., `test/debug_parser.dart` printing input → parsed output), run it to observe real behavior, diagnose from that, then delete the script before finishing. Confidence: 0.75
- After fixing a bug, add unit tests to lock the fix down — "since it broke once already, add a quick unit test to lock it down." New regression tests have proven their worth in-session (caught Dart raw strings not interpolating `#` inside regex patterns). Confidence: 0.8
- Wants the AI coach to remember across app restarts and mid-session settings changes, not just within a session: hydrate the remote coach's conversation from the persisted chat table on provider build. User explicitly probed retention ("Does the coach retain all of its chat?") and approved the hydration fix; visible "Clear chat" remains the explicit memory reset. Confidence: 0.75
- Before changing a shared provider's type/signature (e.g., `Provider` → `FutureProvider`), grep for all usage sites first, then adapt each call site: await `.future` inside actions (within the existing try/catch) and use `valueOrNull` with sensible fallbacks in build methods so the UI renders normally during the async load. Confidence: 0.7
- When replaying persisted chat history to a remote LLM, cap it to a recent window (last ~20 messages, as a named constant) to bound token cost — token spend grows with conversation length. Confidence: 0.65
- Prefers official brand artwork (real bank/e-wallet logos) over stylized monogram placeholders in the finance tracker — explicitly asked to swap in the "proper Logos of the banks" even though the monograms were a documented deliberate choice. Visual fidelity to real branding wins over placeholder consistency. Confidence: 0.85
- Trademark risk of official bank logos is acceptable for this personal, on-device, unpublished app — only a concern if it ever ships to the Play Store. User approved the logo swap after being told the tradeoff. Confidence: 0.7
- When swapping in third-party assets: keep the original filenames so the swap requires zero code changes, back up the originals to the scratchpad first, and validate files against the renderer's limits before committing (e.g., SVGs with no embedded rasters or filters for flutter_svg). Confidence: 0.7
- Prefers current/up-to-date official brand artwork, not just official: after being told the hosted Maya logo was the outdated pre-2022 "PayMaya" wordmark, explicitly asked to hunt online for the updated 2022-rebrand logo ("I would really love if we got the updated maya logo... Try to find online?"). Worth extra searching (Commons metadata + web search) to land the latest official version rather than settling for a stale one. Confidence: 0.7
- Non-trivial PowerShell (pipes, regex, nested quotes) gets mangled when passed as an inline `-Command` one-liner through cmd — write it to a scratchpad `.ps1` file and run it via `powershell -NoProfile -ExecutionPolicy Bypass -File <script>` instead. Confidence: 0.7
- When a brand asset's filename is ambiguous (e.g., `File:Maya logo.svg` could be any "Maya"), verify it's the right entity via upstream metadata (Commons extmetadata ImageDescription) and sanity-check the file's fill colors against the known brand palette before swapping it in. Confidence: 0.65
- When a brand name is shared across regions (e.g., Maribank Singapore by Sea Limited vs Maribank Philippines), confirm which entity the user actually wants before swapping — don't assume. The Filipino market's wallet presets need the local entity's branding, but a same-named bank elsewhere has its own logo. Quick web check on the canonical entity, then source the right asset. Confidence: 0.7
- After completing a task, the user asks the agent for its opinion on what to do next ("What do you think would be the next step?") — they want proactive, prioritized roadmap recommendations rather than waiting to be told. Serve this as an ordered list (quick bug fixes first, then high-value/low-effort items like data-safety, then polish), grounded in verified codebase facts (grep to confirm e.g. an unused dependency or a broken button before asserting it), ending with a concrete "want me to start on X?" offer. Confidence: 0.7
- User reports bugs by symptom only — vague, casual, with no repro steps, error text, or file references ("its not showing the proper thingy its like its showing the code of the markdown", "Clear chat makes my screen black", "The 'Try asking' on chat doesnt go away", "the chat loading on the bottom right and the text box has 'Thinking' is still ongoing even though it already sent a message"). Diagnose by interpreting the described behavior and inspecting/reproducing the relevant code directly; don't ask for technical details they won't provide. This also extends to reporting follow-up regressions right after a fix lands, by observed UI state rather than code. Confidence: 0.85
- Reports missing features as terse, context-free one-liners too ("Wallet has no crud"), not just bugs — expects the agent to inspect the codebase and diff the data layer against the UI to find what's actually missing (e.g., the repository has the method but no screen exposes it), then implement the full CRUD/feature, rather than asking for specifics. Confidence: 0.7
- The finance tracker is privacy-first as a hard design constraint: backup export/import was approved specifically because it stays fully on-device (JSON via file picker/share sheet, "no cloud involved"), and the biometric app lock as fitting a privacy-first money app. Don't propose cloud storage or sync for user data. Confidence: 0.7
- Any surface that displays LLM output must render markdown properly (flutter_markdown `MarkdownBody` with a styleSheet matched to the app theme), never raw markdown source — user complained the coach chat was "showing the code of the markdown". Confidence: 0.7
- When a third-party API integration is uncertain or failing, actively probe the live endpoint over HTTP (curl) — endpoint/route discovery, model listing, connectivity ping — using the user's real key, and report concrete results (status + body) as evidence before changing config, rather than guessing URLs/models from memory. Confidence: 0.75
- Comfortable pasting real API keys/secrets into chat for the agent to test against live endpoints ("Try it please"). When they suspect a value is hardcoded (e.g., it survives hot restart), expects the agent to grep the source and confirm it's only in on-device persistence (SharedPreferences), not in code. Confidence: 0.7
- Prefers curated picker UI over free-text fields for discrete AI-config choices: model selection via a bottom-sheet picker grouped by family with friendly name + tagline + raw ID, and provider-aware lists (different models per provider) rather than one flat list or a raw ID text field. Confidence: 0.7
- Onboarding should offer an explicit provider choice (a picker across all remote providers) rather than defaulting to a single one like CommandCode — user explicitly asked to pick their provider during onboarding. Confidence: 0.7
- When fixing third-party Dart package API mismatches (file_picker, local_auth, etc.), reads the exact installed version's source from the pub cache to confirm the current signatures before editing call sites, rather than guessing from docs or memory. Confidence: 0.7
- When a fix touches Android/native code (e.g., switching MainActivity to FlutterFragmentActivity), also runs `flutter build apk --debug` to verify the native layer compiles — verification isn't limited to `flutter analyze` + `flutter test`. Confidence: 0.7
- In Flutter `AlertDialog`/`SimpleDialog` builders, pop with the dialog builder's own context (`builder: (dialogCtx) => ...`; `Navigator.pop(dialogCtx, ...)`), never the screen's `context` — popping with the screen context pops the underlying route and leaves a black screen / "popped the last page" assertion. Confidence: 0.75
- When a bug is a copy-pasted anti-pattern, grep the whole codebase for the same pattern and fix every occurrence in one pass (not just the reported spot) before running `flutter analyze` + `flutter test`. Confidence: 0.7
- When the user asks whether the app can do something ("What can happen if I have a recurring expense? like the app can do that?"), answers by grounding in what the codebase already supports (existing Subscriptions tab, auto-detection chips), clearly separates current capability from the missing pieces, and ends by offering to build the natural next step as a concrete feature — not by asking a clarifying question. Confidence: 0.7
- Adds DB schema changes as a versioned migration: bump the `_dbVersion` constant, add the new table/column DDL to both `onCreate` and `_onUpgrade` (guarded by `oldVersion < N`), and add an index on any column used in lookups (e.g., `next_due`). Confidence: 0.8
- A new feature screen isn't done until it has a discoverable entry point (e.g., a Dashboard action tile), not just a route registered in the router — checks how sibling screens are reached and wires a visible affordance. Confidence: 0.75
- Prefers strongly-typed collections (`List<Wallet>`) over raw `List`, and removes redundant casts (`w.id as String`, `w.name as String`) once the concrete type is known. Confidence: 0.6
- Expects unit tests to accompany new features, not just bug fixes — asked "I hope you created test" when requesting the background scheduler. Confidence: 0.7
- Values hands-off automation: time-based features (recurring expenses) should run in the background via OS scheduling (WorkManager/BGTaskScheduler), not only on app launch, so due entries are logged even when the app is closed. Confidence: 0.7
- Wants the AI coach to perform data-entry actions from natural conversation (e.g., "I spend 100 every day on commute" → create a recurring entry), not just give advice — explicitly asked "can the AI add it if we talked about it?" and expects it to handle complex debt terms such as due days, exact decimal amounts, and remaining installment counts. Values Gemini/AI as the natural-language parser that returns structured action data (such as JSON) to generate the confirmation card, rather than relying only on rigid local pattern matching; retain local validation against the original message and require confirmation before saving. The AI's freedom spans the full CRUD surface — wallets, debts, goals, income, expenses, recurring entries (create/edit/delete and add/pay-down) — not scoped to a single feature. Pushes back when a plan narrows the AI to one domain ("don't just let it end on Debt"). Confidence: 0.9
- When adding engine/service logic that needs tests, introduces a test seam (e.g., an injectable/overridable DB path) and tests against in-memory SQLite via `sqflite_common_ffi` rather than a real on-disk DB. Confidence: 0.6
- Design aesthetic: "The Quiet Ledger" — flat and tonal (no shadows, gradients, or glass); depth is conveyed purely by tonal surface layering, and any future shadow/elevation must be earned by a genuinely floating element. One deep-green accent used sparingly; amber/red appear only when the data demands them. Confidence: 0.8
- Borders are semantic, never decorative — a border exists only to carry meaning (selection, warning, or a wallet's brand color), never as decoration around a resting card. Confidence: 0.75
- Typography is number-first: when a screen shows a money figure, the number is the largest element on it; small uppercase structural labels explain the number and never compete with it. Confidence: 0.7
- Uses the impeccable skill's `init` and `document` flows, loading their playbook reference files first (e.g. `reference/init.md`, `reference/document.md`): `init` writes PRODUCT.md capturing product truth only (platform, users, positioning, capabilities, constraints, principles — not buildPath or design), and `document` writes DESIGN.md plus a `.impeccable/design.json` sidecar (tonal ramps, motion tokens, self-contained component snippets). Confidence: 0.8
- Product/design records are written from verifiable codebase facts only — never fabricates testimonials, press, benchmarks, or user data, and leaves undecided items open rather than guessing. Confidence: 0.75
- When capturing product/design truth, probes only the gaps the codebase doesn't already answer — asks what's genuinely undecided and doesn't re-ask what the repo already establishes. Confidence: 0.7
- Uses `ask_user_question` with several questions at once (e.g., three), each with 2-4 concrete options and a "Recommended" badge, for product/positioning decisions and design-direction choices — not just single design questions. Confidence: 0.7
- When the plan calls for a web search or online asset lookup, actually run it — don't stall on meta-commentary about trademark risk, copyright, or "I can't reliably reproduce this from memory." If a lookup is the next step, do the lookup. The user flagged this as a recurring stall: "When you do a search you stop" (i.e., the assistant announces it will search and then never does). Surface uncertainty in the *result*, not as a pre-search disclaimer. Confidence: 0.8
- When about to manually test a feature, wants a structured testing checklist first (checkbox items grouped by functional area, covering happy paths and recent bug regressions), not just a "done, it works" summary — explicitly asked "I'm gonna test it. Give me a checklist". Confidence: 0.7
- Before answering a question about the project, expects the agent to first run a thorough codebase-exploration pass to build context ("analyze Project Code to get Context. so you can answer my question properly") — answers to project questions should be grounded in the actual repo (architecture, features, gaps), not given cold from memory or answered with clarifying questions. Confidence: 0.6
- Expects in-flight work to survive navigation: switching tabs/screens mid-operation must not silently cancel it (reported "While in AI chat, I move to another screen. When I went back its cancelled" — the reply that was still "thinking" never landed). Long-running operations' transient state (busy flag, pending action cards, the in-flight Future) should be lifted out of the widget into a provider-level session so a ShellRoute-disposed screen doesn't kill the request; on return the completed result should be there. Confidence: 0.6
- Async UI state must be self-healing and wedge-proof: a shared `busy`/in-flight flag must be cleared via `whenComplete` plus a defensive try/catch around the in-flight setup (so an error escaping `_sendNow`'s own handling can never leave it stuck), and a stale `busy=true` with no request in flight should be self-healed at the top of the next build frame — so disabled inputs/buttons recover even after a hot reload that carried over a wedged provider state from an older buggy build. When the user reports "the send button is not working," the button wiring is usually fine; the root cause is the session believing a request is still in flight. Confidence: 0.7
- In Riverpod, never write to a provider from inside `build` or a getter that runs during build (`.state = ...` triggers "Tried to modify a provider while the widget tree was building" and aborts the frame). Give providers an eager, non-null initial state (`StateProvider<T>((_) => T())`) and mutate the held object in place (repaint via `setState`) instead — a lazy `?? (notifier.state = ...)` write only survives hot reload's stale `static` value and crashes on a cold start. Confidence: 0.8
- Preserve exact monetary amounts, including cents, for calculations, wallet UI displays, and other non-privacy data flows; rounding should be opt-in and limited to an explicitly enabled privacy/anonymization mode, never silently applied to real balances. Confidence: 0.95
- Accepted AI action cards must persist the same user-visible entity details they previewed (especially wallet name/preset); confirming a BDO card must never silently create or label it as GCash or another default/selected wallet. Confidence: 0.85
- Prefers proof-of-concept experiments before committing to a full implementation — "Create a Test first, and lets see how would it work before implementing." For uncertain integrations (e.g., getting Gemini to return structured action JSON), the user wants a throwaway probe script that builds a real prompt, captures the live model's output, and reports feasibility, with application code untouched until the experiment validates the approach. Confidence: 0.85
- When a proposed abstraction doesn't match the user's real situation, they push back by walking through concrete personal-domain examples (specific banks, products, billing mechanics) rather than naming missing fields — expects the model to be redesigned around their actual categories (e.g., Atome's cut-off → bill → 10-day-due-date cycle vs Maya Black's minimum-payment-due vs cash-loan fixed due-day amortization), not flattened into a single generic shape. Confidence: 0.8
- For unfamiliar real-world financial products the user mentions by name, actively research them (web search the actual product's billing/payment mechanics from primary sources — help/support pages, official docs) before designing the data model, rather than reasoning from generic "credit card" assumptions. Confidence: 0.75
- After explaining how a feature should model their reality, ends with "Do you get it now?" — wants explicit acknowledgment that the proposed model maps onto their mental categories before any implementation proceeds. Confidence: 0.7
- Has detailed personal-finance domain context: real Philippine banks (Atome, Maya Black, BDO, GCash), real billing-cycle mechanics (cut-off date, statement generation, due date N days later, minimum payment vs full amortization), and multiple debt types in active use. Domain explanations should be assumed accurate and detailed, not paraphrased away. Confidence: 0.8
- Prefers plan-mode workflow for cross-AI continuation. When external usage limits will hit, asks for everything to be written to a canonical markdown file at `~/.commandcode/plans/<feature>.md` plus a standalone copy-paste prompt so the work can be handed off to another AI. The plan must be self-contained (locked decisions, JSON/contract samples, exact file paths, verification steps, risks) so the receiving AI can resume without further context. Confidence: 0.9
- Plan markdown files for handoff must include a dedicated "Prompt To Use When Handing Off" section with the full brief inline — not a pointer to a separate file — so it's a single copy-paste into the other AI's input box. Confidence: 0.85
- Writes durable plans under `~/.commandcode/plans/` (Windows path `C:/Users/Pia Romualdo/.commandcode/plans/`) — explicit canonical location, not the project root. When the system rejects the tilde form, expand `%USERPROFILE%` or use the absolute path. Confidence: 0.85
- For AI-coach / Gemini integrations, wants the strict-JSON envelope (`{ "reply": string, "actions": [...] }`) documented inline in plans and prompts — the receiving AI should see the full action schema sample, not a summary. Confidence: 0.85
- The coach/AI action validator policy: if Gemini returns an amount that differs from what the user said, drop the action and surface a yellow chat notice "Ignored N action(s): AI suggested a different amount." The user's exact amount always wins — no silent override, no trust-the-AI option. Confidence: 0.9
- For destructive coach actions (delete debt, delete goal, delete recurring), hard-delete is acceptable but must require an `AlertDialog` confirmation before mutating — wallet deletes are soft (archive). Warning chip "This permanently removes the record" is part of the contract. Confidence: 0.85
- For fixed-schedule debts (cash-loan style, with `dueDay` + `remainingPayments`), each `UpdateDebtBalanceAction` decrements the remaining count; when it hits 0, set `paidOff = true` and clear `dueDay` / `remainingPayments` / `schedule = none`. This decrement-on-payment behavior is part of the locked product spec, not a per-call decision. Confidence: 0.85
- Gemini integration settings: uses `responseMimeType: "application/json"` for structured-output mode, `thinkingBudget: 0` to disable thinking, and a `useActionSchema` flag on `RemoteCoach` so the legacy text-mode prompt remains available for "Test API" pings. OpenAI-shape providers use the same schema in the system prompt and parse the first well-formed JSON block defensively. Confidence: 0.8
- Likes documenting the live-probe result that proves a design works — a throwaway script (`scratchpad/gemini_action_probe.dart`) with two baked-in messages (ambiguous → `actions: []`, full → strict JSON) so the handoff AI can re-run it once with a working key, then delete the probe before committing. Confidence: 0.75
- Plan verification command is `flutter analyze && flutter test` (expect 0 issues, all existing + new tests pass). For native-touching fixes add `flutter build apk --debug`. Schema-version DB changes must be verified by cold-starting the app to confirm the migration runs without losing data. Confidence: 0.85
